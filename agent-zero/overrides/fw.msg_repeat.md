The previous assistant turn repeated the same response or the same tool call with equivalent arguments. This call is now blocked and MUST NOT be emitted again. Read the latest tool result and advance the task by choosing exactly one option: call a different tool; call the same tool with materially different arguments; perform a state-changing browser action such as scroll, click, evaluate, or navigate to a different target; or finish with the response tool if no further action is useful. If a browser navigate result kept currentUrl on another page, treat it as a redirect, authentication requirement, or access block: inspect state/content once or report the blocker, but never navigate to the same URL again. Do not merely restate the plan.
